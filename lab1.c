#include <stdio.h>

int main()
{
    float a,b,c;
    suma = a
    c = 0
    while(suma!=0){
        c+= suma%10;
        suma/=10
    }
    if (suma = b)
        printf("Сумма цифр =",b)
    if (suma>b)
        printf(suma)
    else
        printf(suma+b)
}